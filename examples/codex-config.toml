[mcp_servers.solidworks]
type = "stdio"
command = "python"
args = ["-m", "solidworks_mcp"]

# If you prefer running from a repository path instead of the installed module:
# command = "<path-to-python>"
# args = ["<path-to-repo>/server.py"]
